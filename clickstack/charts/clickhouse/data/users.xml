<?xml version="1.0"?>
<clickhouse>
    <profiles>
        <default>
            <max_memory_usage>10000000000</max_memory_usage>
            <use_uncompressed_cache>0</use_uncompressed_cache>
            <load_balancing>in_order</load_balancing>
            <log_queries>1</log_queries>
        </default>
        <readonly>
            <readonly>2</readonly>
        </readonly>
    </profiles>

    <users>
        {{- $username := include "clickhouse.credentials.username" . }}
        {{- $password := include "clickhouse.credentials.password" . }}
        {{- $useExternal := include "clickhouse.credentials.useExternal" . }}
        {{- if eq $username "default" }}
        <!-- Using default user with configured password -->
        <default>
            {{- if ne $useExternal "true" }}
            <password>{{ $password }}</password>
            {{- else }}
            <!-- Password from external secret: {{ include "clickhouse.credentials.secretName" . }} -->
            <password></password>
            {{- end }}
            <profile>default</profile>
            <networks>
                <ip>::1</ip>
                <ip>127.0.0.1</ip>
                {{- if .Values.config.clusterCidrs }}
                {{- range .Values.config.clusterCidrs }}
                <ip>{{ . }}</ip>
                {{- end }}
                {{- end }}
                <host_regexp>.*\.svc\.cluster\.local$</host_regexp>
            </networks>
            <quota>default</quota>
            <grants>
                <query>GRANT ALL ON *.*</query>
            </grants>
        </default>
        {{- else }}
        <!-- Custom user: {{ $username }} -->
        <default>
            <password></password>
            <profile>default</profile>
            <networks>
                <ip>::1</ip>
                <ip>127.0.0.1</ip>
            </networks>
            <quota>default</quota>
        </default>
        <{{ $username }}>
            {{- if ne $useExternal "true" }}
            <password>{{ $password }}</password>
            {{- else }}
            <!-- Password from external secret: {{ include "clickhouse.credentials.secretName" . }} -->
            <password></password>
            {{- end }}
            <networks>
                {{- if .Values.config.clusterCidrs }}
                {{- range .Values.config.clusterCidrs }}
                <ip>{{ . }}</ip>
                {{- end }}
                {{- else }}
                <ip>10.0.0.0/8</ip>
                {{- end }}
                <host_regexp>.*\.svc\.cluster\.local$</host_regexp>
            </networks>
            <profile>default</profile>
            <quota>default</quota>
            <grants>
                <query>GRANT SHOW ON *.*</query>
                <query>GRANT SELECT ON system.*</query>
                <query>GRANT SELECT ON default.*</query>
                <query>GRANT SELECT,INSERT,CREATE,SHOW ON default.*</query>
            </grants>
        </{{ $username }}>
        {{- end }}
    </users>

    <quotas>
        <default>
            <interval>
                <duration>3600</duration>
                <queries>0</queries>
                <errors>0</errors>
                <result_rows>0</result_rows>
                <read_rows>0</read_rows>
                <execution_time>0</execution_time>
            </interval>
        </default>
    </quotas>
</clickhouse>
